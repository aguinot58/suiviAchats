{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id utilsateur</th>
                <td>{{ utilisateur.idUser }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nomUser }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ utilisateur.prenomUser }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ utilisateur.mailUser }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.mdpUser }}</td>
            </tr>
            <tr>
                <th>Id rôle</th>
                <td>{{ utilisateur.idRole }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateurs_index') }}">Retour</a>

    <a href="{{ path('app_utilisateurs_edit', {'idUser': utilisateur.idUser}) }}">Modifier</a>

    {{ include('utilisateurs/_delete_form.html.twig') }}
{% endblock %}
