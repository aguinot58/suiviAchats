<form method="post" action="{{ path('app_roles_delete', {'idRole': role.idRole}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer cet enregistrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.idRole) }}">
    <button class="btn">Supprimer</button>
</form>
