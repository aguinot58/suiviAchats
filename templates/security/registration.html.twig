{% extends "base.html.twig" %}
{% block body %}
    <h1>Inscription</h1>
    {{ form_start(form) }}
    {{ form_row(form.mailUser, {'label': 'Adresse email'}) }}
    {{ form_row(form.Username, {'label': 'Prénom'}) }}
    {{ form_row(form.surName, {'label': 'Nom de famille'}) }}
    {{ form_row(form.password, {'label': 'Mot de passe'}) }}
    {{ form_row(form.confirmMdp, {'label': 'Confirmer le mot de passe'}) }}
    {{ form_row(form.idRole, {'label': 'champs a supprimer'}) }}
    <button type="submit" class=" btn btn-success">Valider</button>
    {{ form_end(form) }}
{% endblock %}