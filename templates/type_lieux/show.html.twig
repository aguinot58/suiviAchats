{% extends 'base.html.twig' %}

{% block title %}Type Lieux{% endblock %}

{% block body %}
    <div class="container text-center">
        <h1 class="mt-5 mb-5">Type Lieux</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id Type Lieu</th>
                    <td>{{ type_lieux.idTypeLieu }}</td>
                </tr>
                <tr>
                    <th>Nom Type Lieu</th>
                    <td>{{ type_lieux.nomTypeLieu }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_type_lieux_index') }}">Retour</a>

        <a href="{{ path('app_type_lieux_edit', {'idTypeLieu': type_lieux.idTypeLieu}) }}">Modifier</a>

        {{ include('type_lieux/_delete_form.html.twig') }}
    </div>
{% endblock %}
